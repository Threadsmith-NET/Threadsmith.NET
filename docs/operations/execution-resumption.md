# Execution orchestration and resumption

An approved implementation plan continues in the same run. Every accepted or corrected `propose_plan` payload represents one complete, independently valid plan tranche, and approval authorizes that whole tranche only; it does not authorize repository writes or later tranches. Implementation is incremental after approval: the host selects the earliest incomplete step and requests separately reviewable mutation batches for that step. After every step in a tranche validates, the host records a nonterminal `PlanContinuationPending` boundary and returns the same objective to ordinary evidence/planning. The model either proposes one next tranche or calls `complete_objective` to confirm that no requested work remains. Before approval, every proposed plan passes cheap host-owned sanity checks; repairable plan-scope failures return to the model for bounded complete-plan revision instead of creating a review prompt.

## Normal flow

1. The model proposes complete content for the next cohesive plan tranche through `propose_plan`. The host supplies schema version, revision and step IDs; the model supplies the summary, ordered steps/file intents, validation expectations, risks and questions. Corrective retries and revisions replace that complete candidate using the same flat content shape. Do not submit the old metadata fields, a `plan` wrapper, nested plans, or a speculative outline of all later tranches.
2. The host runs plan sanity checks over structured schema-2 file intents, path confinement, current baseline existence, protected/secret/Git targets, generated/binary risk, lifecycle/configuration/dependency/test-deletion risk, and scope bounds.
3. Repairable sanity failures are recorded and fed back as plan-revision evidence. Non-repairable path/trust/protected-policy failures fail closed.
4. A passing plan is manually approved or policy-auto-approved according to `/plan-policy` / `planning:approvalPolicy`; auto-approval records policy, risk, revision, and bounded scope summary.
5. The host enters implementation, selects the earliest incomplete approved step, emits visible mutation-preview preparation status, supplies eligible bounded source evidence, and advertises proposal-only `propose_mutations` plus `request_replan` when incremental planning is enabled. Replanning is an exclusive decision, not another inspection or mutation tool. The request carries compact progress and the configured soft mutation, file, and content-size targets; those targets guide model output but do not alter approved scope or hard workspace limits.
6. The model proposes only the next coherent batch for the active step and may include the optional `stepComplete` hint. A large step can span batches; later plan steps are not generated as mutations yet.
7. Every batch is validated against the active approved step, paths, trust, budgets, and the current mutation baseline. C# `RenameSymbol` proposals are expanded through the semantic mutation engine before staging when semantic state is available.
8. Proposed `.cs` mutations are applied to an in-memory overlay and screened by Roslyn before staging or approval. Blocking syntax/fast compilation diagnostics return to the model for proposal repair; repository files remain unchanged.
9. The batch stages atomically only after proposal validation and pre-mutation cheap gates pass. Its exact diff is recorded and shown before policy evaluation.
10. A prompted or host-policy mutation decision is recorded separately for that exact batch. Plan approval and earlier mutation decisions do not authorize it.
11. The host builds or semantically evaluates the exact pre-mutation workspace according to configured post-approval `validation:stages` and durably records `BaselineCapture`.
12. A write-ahead commit intent is recorded, the authorized transaction is applied once, and its result is reconciled. The transactional mutation baseline is promoted so later batches use current bytes.
13. Configured post-mutation validation runs in order: semantic, compile, diagnostics, and/or affected tests. Build/test validation remains authoritative when configured. A failure enters the existing bounded correction path for the same active step and preserves the original batch's completion intent.
14. After a fully authorized passing batch, `stepComplete: true` completes only the active step; `false` or an omitted hint requests another batch for it. A completion-only response requires current passing evidence from a fully applied batch of that same step. A partial authorization validates what applied and pauses at `ContinuationPending` without inheriting the completion hint or automatically regenerating rejected work.
15. The host advances to the next approved step without another plan approval. Supported completion of every step produces a validated plan boundary, not necessarily a terminal objective outcome.
16. With `planning:incrementalPlans:enabled`, the host archives the authoritative plan progress, re-enters evidence collection on the same run, and supplies configured soft step/file targets plus the number of plans used, including interrupted plans. The latest cumulative receipt is included as current-run host context even in stateless or governed-memory-only mode; ordinary history retains the full user/assistant exchange for later requests. Current repository evidence complements that receipt; the host does not require a model-authored meta-plan or summary packet.
17. If work remains, one new `propose_plan` call starts the ordinary sanity, approval, step, batch, mutation, and validation path again. If the objective is complete, `complete_objective` must be called with exactly `{}`. The host then runs the configured validation stages once more over the cumulative affected paths, projects, project inventory, and stage requirements from every tranche. Only a passing result records terminal success; failure records an accurate terminal failure. The final diff remains the net result relative to objective-start bytes rather than a concatenation of batch patches. Extra completion fields receive the existing actionable corrective feedback.
18. There is no plan-count limit. Further tranches and replacement plans use the existing cumulative execution budget, cancellation, plan policy, and exact-diff authorization. An ordinary explanation of remaining work or a clarification question ends the current assessment without a success outcome; explicit resume remains available.

Headless callers must provide a policy-authorized decision or explicit host input. They never self-approve.

## Mid-tranche replanning

During an implementation or correction turn, `request_replan` alone with `{"reason":"..."}` records `PlanReplanningPending` without staging or applying another mutation. The host accepts safe property casing, sanitizes and bounds the reason using the existing plan summary limit, and routes malformed or mixed decisions through existing corrective feedback. This capability uses `planning:incrementalPlans:enabled`; disabled incremental planning does not advertise it.

The same session completion observer re-enters the ordinary evidence/planning loop with current repository tools, the interrupted plan, the reason, and the authoritative receipt. Applied bytes, completed-step evidence, cumulative validation scope, budget use, and original-file/net-diff evidence remain intact. The model proposes a complete replacement tranche for remaining work through the normal formatter, sanity checks, and plan policy. Previous plan or mutation approval cannot authorize the replacement's writes. `complete_objective` is withheld and rejected at this unfinished boundary.

Replanning is allowed only between settled transactions. An existing diagnostic baseline and any unresolved failure evidence are retained; the diagnostic basis is not recaptured from already-mutated code, even for a later tranche. Transactional baselines still advance to current bytes. The host never implicitly reverses applied changes. Questions and blockers pause for explicit resume, and restart reattaches this same conversation loop without replaying applied mutations.

## Cancellation

Cancellation before application leaves repository bytes unchanged. Cancellation around a lifecycle commit reconciles exact endpoint identities: a fully untouched or compensated set records the operation as `RolledBack`, while conflicted or indeterminate effects record recovery-required state. Cancelled build/test output is not admitted as authoritative.

## Explicit resume

Use the shared `ResumeRunCommand` (the interactive `/resume` surface may call the same command) only for a nonterminal interrupted or cancelled execution. Resume revalidates session/run/workspace identity, plan revision and hash, plan ordinal, diagnostic and transactional baselines, active/completed step progress, continuation artifact integrity, and operation state.

After revalidation, an interrupted pre-write baseline capture or a cancelled lifecycle commit with proven compensation returns to mutation approval so fresh authorization is required before retrying. A checkpoint proving mutation application advances by rerunning validation and continuation/outcome assembly only; it never reapplies the committed mutation. Interruption during a later implementation model turn resumes generation for the persisted active step and next batch ordinal without replaying prior proposals, approvals, transactions, or validation. A resumed validation failure may stage a new correction for the same exact-diff review loop. Initial start and resume share the run continuation gate, so concurrent attempts cannot start the same proposal twice. Session reattachment uses the durable request's workspace identity and the same workspace-scoped semantic admission gate used by publication; aliases therefore cannot publish through a different gate while resume is advancing or attaching the observer.

`ContinuationPending` after partial mutation authorization is a deliberate pause, not an interrupted-effect recovery. In the interactive workflow, `/validation retry` explicitly continues from promoted current bytes. The host generates a fresh candidate and requires a fresh exact-diff decision; it does not revive the partially rejected candidate.

`PlanContinuationPending` is different: the current plan has passed and the host is assessing the remaining objective. Its cumulative original-file evidence, applied files, lifecycle changes, validation scope, compact validation summary, budget use, and net diff remain on the same execution. Detailed validation results remain separate artifacts rather than being copied into every continuation-state snapshot. A subsequent approved plan replaces only current-plan scope and step progress. `ResumeRunCommand` revalidates execution state, then reattaches the existing session completion/planning loop from the durable approved request and progress, including after restart. It does not replay completed mutations. Ordinary questions or blockers end the current assessment without terminal success and leave this boundary available for explicit resume.

Resume is denied when:

- the execution is terminal;
- checkpoint or artifact schema is unsupported;
- a required artifact is absent or corrupt;
- repository, solution, plan, plan-approval provenance, trust, mutation policy, or baseline identity changed;
- a pending side effect cannot be reconciled safely.

A denial does not adapt or merge repository state. Start a fresh request/plan or perform explicit recovery.

## Durable evidence

Checkpoint rows contain bounded host-owned DTOs and artifact references. Schema-2 execution checkpoints retain the active step, step and batch ordinals, batch purpose, completion hint, supported completed-step IDs, promoted mutation-baseline generation, and next legal action; their referenced continuation-state artifacts retain cumulative budget usage, affected validation scope, applied paths/lifecycle facts, the latest validation result, and a failed-validation count. New state snapshots do not reserialize historical validation payloads or batch diffs. The reader uses legacy validation history only to migrate the compact failure count and safely ignores obsolete batch-diff fields; subsequent writes omit both histories. Plan sanity summaries, auto-approval provenance, exact diffs, continuation state, original touched-file evidence, baseline captures, and validation evidence are sanitized and content-addressed. Terminal outcomes report only steps with supported completion evidence and retain one cumulative net final diff when trustworthy original bytes are available; historical state without that evidence does not invent a net diff. Hidden reasoning, secrets, raw provider payloads, and unbounded process output are never checkpoint content.
