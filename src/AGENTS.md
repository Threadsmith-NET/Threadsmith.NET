# AGENTS.md — src/ (Product Source)

> **Scope:** All `Threadsmith.*` product projects under `src/`. This doc governs how agents work with the 21 product projects and their dependency graph.

## Purpose

Host the 21 Threadsmith.NET product projects implementing a .NET-native coding harness. The solution file is `src/Threadsmith.sln`; all test projects are grouped under its `Tests` solution folder.

## Ownership

- Owns product-source layering, dependency direction, and cross-project implementation contracts.
- `Threadsmith.App` is the composition root — references all other product projects.
- `Threadsmith.Core`, `Threadsmith.Extensions.Abstractions`, and the isolated `Threadsmith.Scripting.Worker` reference no other `Threadsmith.*` project.

## Local Contracts

### Dependency layers (enforced by `DependencyDirectionTests.cs`)

Projects are organized in dependency layers. A project may only reference projects in layers below it:

- **Layer 0 (roots/deployable isolation):** `Threadsmith.Core`, `Threadsmith.Extensions.Abstractions`, `Threadsmith.Scripting.Worker` — reference no other `Threadsmith.*` project. Only the worker may reference Roslyn scripting; it communicates through bounded JSON over standard I/O.
- **Layer 1:** `Threadsmith.Telemetry`, `Threadsmith.Models` — reference Core only.
- **Layer 2:** `Threadsmith.Persistence`, `Threadsmith.Context`, `Threadsmith.Models.OpenAiCompatible`, `Threadsmith.Models.OpenAiCodex` — reference Core + Layer 1. Concrete model-provider projects depend one-way on neutral Models and isolate protocol/SDK/OAuth types.
- **Layer 3:** `Threadsmith.Tools`, `Threadsmith.DotNet`, `Threadsmith.Workspaces` — reference Core + Layer 2.
- **Layer 4:** `Threadsmith.Validation`, `Threadsmith.Execution`, `Threadsmith.Skills`, `Threadsmith.Extensions.Runtime` — reference Core + layers 1–3. Skills may reference neutral Context/Models/Telemetry/Tools but never Execution, Workspaces, Validation, MCP, terminal adapters, or provider/persistence implementations; it returns only Core host-action proposals and checkpoints.
- **Layer 5:** `Threadsmith.Tui`, `Threadsmith.Cli`, `Threadsmith.Mcp`, `Threadsmith.Hooks` — reference Core + layers 1–4. Hooks owns adapter-neutral policy/coordination and references only Core + Tools; TUI/CLI consume host-owned commands/results.
- **Layer 6 (composition root):** `Threadsmith.App` — references all product projects.

### Forbidden package references (§8.1)

- `Threadsmith.Core`: no terminal UI libraries, `Microsoft.CodeAnalysis`, `OpenAI`, or `System.Net.Http`.
- `Threadsmith.Extensions.Abstractions`: no terminal UI libraries, `Microsoft.CodeAnalysis`, `OpenAI`, or `Microsoft.Data.Sqlite`.
- `Threadsmith.Tui`: no `Microsoft.Data.Sqlite`.
- `Threadsmith.Skills`: no terminal libraries, Roslyn, provider SDKs, `Microsoft.Data.Sqlite`, or executable package activation.
- `Threadsmith.Scripting.Worker`: Roslyn scripting only; no `Threadsmith.*` project references.

### Adding a new project

- Place under `src/Threadsmith.<Name>/`.
- Add to `src/Threadsmith.sln`.
- Add to `DependencyDirectionTests.cs` `_productProjects` array and `_allowedGraph`.
- Add to `Directory.Packages.props` for any new packages.
- Update this AGENTS.md dependency layers.

## Work Guidance

- Follow `docs/guardrails/portable-csharp-guardrails.md` (G-1…G-31) before writing C#.
- Follow the dependency direction rules — run `dotnet test` on `Threadsmith.Architecture.Tests` after any project reference change.
- Central Package Management: add packages in `Directory.Packages.props`, never inline.
- Nullable enabled, no `!` suppression.
- Constructor injection only; no property injection.
- Return host-owned DTOs across subsystem boundaries — no SDK, Roslyn, extension, PrettyPrompt, or Spectre.Console types in domain events or persistent state. `ApprovalRequested.Action` is presentation-only; semantic routing uses the closed host-owned `ApprovalRequestKind`, and legacy `Unspecified` events remain unclassified rather than inferring authority from text.
- Model endpoints are selected per request from `ConfiguredModelCatalog`; the effective catalog binds selected profiles to immutable compiled-provider registrations after bounded user/repository ID-based layering and allowlisted polymorphic deserialization. Sensitivity is classified before selection and asserted again at the provider boundary. Shared session model preferences track the effective profile, start from its validated reasoning default, and durably reset reasoning to `None` when resolution changes profile. OpenAI reasoning compatibility is closed/versioned, projects provider-neutral selectable/always-on/unsupported capability, preserves legacy request behavior when absent, and prevents repository overrides from changing an inherited compatibility mode/version. Explicit process-scoped raw model exchange diagnostics wrap the provider-neutral `IModelProvider` boundary only when requested, never persist an enabled setting, require repository-local output paths to be untracked, unstaged, and effectively Git-ignored, and must not record HTTP authorization headers or credentials.
- Remote model endpoints require HTTPS. Plain HTTP is limited to loopback local-provider endpoints.
- Shared prohibited-path globs are compiled and cached by `RepositoryPathPolicy`.
- Initial model requests use the general workload and may answer conversationally or invoke authorized read-only tools plus compiled conversation-available `run_process` when its configured shell is allowlisted and trusted machine/user configuration disables per-call process approval. Process execution remains `ExecutesCode`; trust, executable allowlisting, optional trusted approval, policy, hooks, bounds, cancellation, and audit remain authoritative. The provider receives host-owned function definitions, including `propose_plan`; only a validated plan proposal transitions the run into governed change planning. No mutation tool is exposed before approval. The advertised tool set is filtered through the effective repository invocation policy (insufficient repository trust, `tools:deny`, non-empty `tools:allow`, and `tools:requireApproval` withhold the tool from the model so it cannot select a tool the host would reject), in addition to the existing per-invocation policy gate. Semantic tools (`find_symbol`, `find_references`, `find_implementations`, `call_hierarchy`, `symbol_impact`, `csharp_pattern_search`, and `generated_code_query`) are offered only when a workspace is loaded; without a workspace the model receives file-system tools only so it cannot loop on a tool that will always fail. When an advertised semantic tool covers the repository question, the stable host policy requires it before text search; text fallback is allowed only when none applies or the attempted semantic tool fails or explicitly reports incomplete/degraded evidence. With a loaded workspace and advertised `find_symbol`, first-pass `search` calls that look like C# symbol/type/class or `.cs` filename lookup are rejected before execution and returned as corrective evidence directing the model to the semantic tool. Repeated tool calls with identical arguments are not re-invoked; the model is fed a duplicate-stop evidence entry and must use the earlier result or answer directly. Once target, applicable instructions, and material impact resolve the requested change without a correctness ambiguity, planning must converge to `propose_plan` rather than investigate unrelated patterns or references. Initial evidence collection advertises inspection tools for at most the configured `execution:maxPlanningToolRounds` (default four); later continuation requests retain only `propose_plan`, and any stale inspection call is not executed. A same-phase malformed `propose_plan` argument payload receives a bounded schema repair loop, controlled by `execution:maxPlanProposalRepairAttempts` and defaulting to three repair opportunities, instead of immediately failing the run. Tool status is published by the pipeline via `ToolInvocationStarted`/`ToolInvocationCompleted`, not as model output text; optional built-in activity detail comes only from a reviewed typed-input formatter and is sanitized, single-line normalized, and bounded before publication.
- Lifecycle hooks use the closed Core catalog and result union. Repository declarations require exact external digest approval and remain advisory/fail-open; only repository-excluding managed grants can block eligible pre-actions. The tool pipeline emits before/after boundaries, authoritative durable source events feed advisory after/terminal boundaries, mutation events correlate to one completed-transaction notification, and successful MCP capability publication emits `McpConnected`. Non-cooperative handlers are abandoned at timeout, management tests target only their selected handler, shared TUI/headless surfaces expose management, and migration 6 retains bounded redacted audit without blindly replaying interrupted handlers.
- The optional `csharp_script` tool is disabled by default, requires `FullyTrustedAutomation`, and executes only in a fresh tracked `Threadsmith.Scripting.Worker` process. Tool-level configuration is scalar host data; timeout/cancellation kills the worker tree, output is UTF-8 bounded, prohibited capabilities fail closed, and the worker is not represented as an operating-system security sandbox.
- The optional `web_search` tool is disabled by default and additionally requires user-owned repository-bound outbound consent. Consent schema 2 explicitly covers retrieval; legacy search-only records require re-consent. Search results receive transient opaque repository-bound references that progressively activate the separately registered `web_fetch` schema only on subsequent model rounds of their producing session/run; unrelated runs retain the smaller inventory. Exact direct fetch grants are host-created, short-lived, one-shot, and atomically grouped per intended redirect chain; independent grants cannot authorize each other's redirects. Fetch accepts only public HTTPS textual content, validates every DNS answer and manually authorized redirect, pins each socket to a validated public address while preserving TLS hostname validation, sends no cookies/proxy/ambient credentials, bounds compressed/decoded/parser/extracted content, and returns query-free digest-bearing untrusted evidence. Exact current fetch hosts are admitted as transient host-owned network claims without requiring repository host preconfiguration; ordinary tool deny, trust, consent, URL, DNS/address, redirect, and transport checks remain mandatory. Repository configuration and trust cannot grant consent or direct authorization, and repository-inclusive fetch limits may only narrow repository-excluding trusted ceilings. Authorization and retrieval share one thread-safe effective fetch-options source that rebinds and revokes transient routes with repository tool state; failed repository opens restore the prior repository limits. Under consent schema 3, only fresh raw top-level user intake may issue bounded opaque `userUrlId` references for exact HTTPS candidates; candidates reaching the scan boundary are rejected unless the raw message ends there, and schema 2 remains valid only for legacy search-result and explicit-direct behavior. Current-message authority is message/repository/session/run/policy-generation/expiry-bound, one-shot, non-restorable, and revoked by the next intake or terminal/lifecycle change. A model-proposed direct URL is considered only while fetch is progressively active, uses one serialized TUI-neutral exact approval whose URL-free lifecycle notifications remain process-local and non-durable, grants only the same pending invocation, and returns actionable sanitized `DirectAuthorizationRequired` origin/redacted-path/digest output with exit code 3 without prompting headlessly; reused headless sessions emit tool activity only for the current run. Current-user and inline routes authorize no redirects; `/fetch-authorize` remains the explicit redirect-group/automation boundary.
- MCP profile enum and capability configuration fails closed; only named logical secrets are scoped to a server. Malformed `secretScope` entries fail before transport startup, and accepted configured profile identifiers are converted to stable non-disclosing diagnostic IDs rather than narrowing the profile-ID syntax at secret resolution. SDK-backed stdio disables arbitrary environment inheritance, forwards curated OS startup variables plus explicitly scoped values, applies one profile startup deadline across scoped secret resolution, connection, and handshake, and bounds real child-process shutdown/kill. SSE/streamable-HTTP resolves exact scoped header secrets without retaining them in public profile/status state, exposes endpoint hosts to network policy, and owns HTTP/session disposal. Auto-connect considers only repository-excluding trusted configuration, publishes imported tools to the shared registry for the connection lifetime, and treats profile-local failures as nonfatal; OAuth-enabled HTTP profiles use SDK-advertised discovery, authorization-code + PKCE, state/issuer validation, bearer attachment, and refresh through host-owned browser/callback and per-profile secret-cache adapters. OAuth client secrets must be logical `secrets:` references with exact case-sensitive membership in the profile secret scope before resolution. Configured scopes are an authorization ceiling, an automatically selected callback port remains reserved through discovery and is already listening before browser launch, Unix cache files are private from creation, malformed optional cache state is recoverable, unsupported discovery overrides fail closed, and OAuth cannot combine with static `Authorization` headers. Application composition provides one `McpManager` for startup, repository transitions, shared commands, and shutdown; it projects disconnected/live profiles, validates bounded executable/HTTPS eligibility, serializes generation-fenced lifecycle transitions, suppresses OAuth authorization UX during automatic connections while preserving explicit authentication and extension-free single-JSON headless stdout, closes connection and capability-generation admission before registry replacement/removal, and retunes/continues SDK cleanup across every owned resource within the remaining bounded drain/kill deadline with honest forced-termination outcomes, discovers bounded tools/resources/templates/prompts, delegates repository/schema-bound user-approved imported-tool availability to the shared tool-state authority, redacts explicit resource/prompt output as aggregate-truncation-aware untrusted non-context content, and keeps local logout, timeout-aware unconfirmed same-origin non-redirecting RFC 7009 revocation, and one-identity switch effects distinct. TUI/headless layers see only Core DTOs and cannot access SDK or token-store types.
- Startup persistence applies ordered migrations, including migration 8's repository-bound session catalog/model/usage/clone provenance and legacy durable-session catalog backfill, initializes sanitized artifacts, audits redaction, and performs one configured retention pass. New and cloned lifecycle projections persist their base repository/solution authority before activation so restart replay remains authoritative. Eligible artifact cleanup removes both metadata and the content body; it must never report deletion without performing it. Conversation-message artifacts are excluded from the general age sweep and are deleted only by reference-aware conversation-body retention after the final message reference is detached.
- Extension generations publish their validated tool proxies to the shared catalog as one batch and retain registration ownership per generation until removal. Hot replacement explicitly identifies its predecessor, unrelated dynamic registrations and constructor-supplied built-ins cannot be replaced, failed generation registration leaves no partial catalog entries, and unload uses each proxy's registered tool id without removing a successor.
- Tool scheduling collects the complete sibling tool-call set before execution, derives schema-versioned confined resource claims from validated inputs and the immutable request context, forms stable conflict-free waves, starts reviewed read/read built-ins concurrently under host bounds, and joins model-visible results by original ordinal. Approval, process/code, workflow, semantic-workspace, MCP, extension, unknown, and conflicting calls serialize conservatively; the model cannot author scheduling metadata. Planning model requests may continue through bounded read-only tool rounds over one frozen canonical request prefix. Every call in a round has a unique host correlation id, correlated results are reduced before dispatch when needed to remain within the selected model input budget, and each full result remains governed evidence. Initial conversation scope follows the confined process working directory when it is inside the repository; plan revision and mutation preparation derive the deepest existing common scope from authoritative schema-2 file-intent paths so applicable nested `AGENTS.md` files are included.
- Skills are metadata-first declarative packages, never executable capability sources. Discovery reads only bounded manifests. Claude-style compatibility separately reads only bounded safe `SKILL.md` frontmatter from explicit repository/user roots, reports source format/restrictions honestly, and loads confined strict-UTF-8 content only during deterministic digest activation. Exact enablement stays in repository-excluding policy, and native/compatibility sources share the same skill commands, workflow checkpoints, invocation, cancellation, and digest-fenced resume; `allowed-tools`, scripts, hooks, agents, or fork hints never become authority. Explicit verify/invoke rechecks manifest/assets, revocation, external signer or exact digest/publisher/source trust, separate enablement, safe schemas, phase/trust/tool/model/host compatibility, and current-step content bounds. Procedure tools use the central pipeline; workflow nodes return only known host-action proposals. Saved pins resolve unqualified invocation to one exact installed identity. Scope/id/version/digest remains pinned across migration-5 checkpoints and resume; current session phase/trust/workspace is revalidated at restore boundaries, and generation/status compare-and-swap rejects stale checkpoint writers. Age retention removes terminal skill workflows only. Repository configuration may disable repository discovery but cannot establish signers, allowlists, enablement, or revoke exceptions.
- Cross-turn continuity archives only sanitized accepted-user and final-visible-assistant messages, conservatively classifies repository-session content before model selection, and never relies on an enum default for sensitivity. Conversation-aware, Governed-memory-only, and Stateless request assembly uses host-owned provenance, bounded authoritative promotion, structured validation, deterministic retrieval/invalidation, selected-model-window pressure, sensitivity-aware model selection, and metadata-only observability. Successful host workflow boundaries promote authoritative decisions, unresolved questions, completed work, and governed repository evidence; repository-open and mutation events invalidate dependent memory before a subsequent assembly, while retention detaches expired bodies and deletes artifact content only after its final conversation reference is gone. Hidden reasoning/provider/tool payloads never enter durable conversation state.
- Interactive startup defaults to the process current directory and keeps repository/trust/solution choices behind the shared command boundary; host slash commands never reach the model. `/new`, `/resume`, and `/clone` share one serialized safe-boundary session authority; repository open rebinds that authority and activates a fresh session for the new repository, repository-scoped selector labels are bounded/newest-first, clone state copies governed evidence with inherited usage separated from zeroed clone-local usage, and live execution/provider continuation authority is never copied. Resume rollback restores the source model before republishing its catalog checkpoint.
- Interactive status and prompt labels derive from host-owned model/session/repository state; terminal adapters must not infer or retain independent engine state. Runtime selection is owned by `ActiveModelSelectionService`: repository provider/profile/reasoning memory overrides user defaults when valid, repository-open rebinds effective selection, tools, mutation policy, native/Claude skill roots, lifecycle scope, and persistence targets as one rollback-safe boundary, provider dispatch and context assembly use one immutable captured generation throughout each request, and shared TUI/headless commands persist one complete atomic selection and invalidate stale context inspection after profile changes. Every model/reasoning, tool, solution, and mutation-policy read-modify-replace operation on `.threadsmith/config.json` uses `RepositorySettingsCoordinator` so comments/trailing commas remain accepted, linked settings paths fail closed, and concurrent preference changes preserve one another. Provider usage is normalized into the thread-safe `SessionUsageProjection` once per host request identity so presentation reads immutable cumulative snapshots without recounting stream chunks. Completed turns checkpoint current usage, message count, preview, activity time, and model selection into the durable session catalog; snapshots distinguish an untouched session from a provider-reported zero.
- Ordinary conversation is not charged to an execution-token budget; cancellation, tool-call limits, per-tool timeouts, and continuation-round limits bound it. Mutation-proposal operations receive fresh configured budget scopes. Cumulative session usage remains telemetry and never exhausts a conversation quota. Approved plans delegate into the serial `IExecutionOrchestrator` in the same run. The delegation coordinator may wrap proven non-overlapping approved steps in one host-created child layer: children remain in-process observed asynchronous runs with narrower frozen model/tool/trust/context/budget policy, implementation workers use managed detached worktrees, and selected results return to the serial parent transactional/validation path. Scheduler concurrency is bounded by actual parent-run identity across delegations, returned failed outcomes apply their declared failure policy, and assignment scope rejects every `.`/`..` path segment before canonicalization. No child creates descendants or merges itself. Implementation/correction requests use the `CodeEdit` workload, accepted-plan state, immutable diagnostic-baseline facts, current promoted mutation-baseline hashes, eligible bounded read-only tools, and proposal-only `propose_mutations`; its closed schema accepts only plan-step ids (canonical `{value}` objects or bare UUID strings) plus model-authored change content, including `CreateFile.content.text` as the authoritative file body that the host maps into staged replacement text. Mutation tool presentation must name canonical fields (`mutationSet.rationale`, mutation item `type`, `relativePath`, and `baselineSha256`) and explicitly reject plan/legacy synonyms (`kind`, `path`, `baselineHash`, and per-item prose fields), while the host assigns session/run/workspace/baseline/mutation-set/mutation identities before validation and staging. Before staging, proposed `.cs` mutations are applied to a host-owned in-memory overlay and screened by `IPreMutationAnalyzer`; bounded non-cancellable `SemanticCheckStarted`/`SemanticCheckCompleted` activity events expose syntax/compilation progress for presentation, blocking Roslyn syntax/compilation diagnostics are returned as repair evidence, trusted/isolated analyzer checks may participate when available, ordinary repository analyzers/source generators degrade to omissions, and `PreMutationAnalysisCompleted` records bounded counts without source text. For C# symbol renames, `RenameSymbol` proposals with semantic symbol id and new identifier are expanded through the loaded semantic mutation engine before transactional staging; degraded confidence or engine warnings publish non-error semantic mutation warnings before preview approval, and optional declaration-file moves remain explicit `MoveFile` lifecycle mutations. For model-authored `ReplaceText`, exact expected text is mandatory; a mismatched offset is corrected against the rolling immutable/staged text only when that expected text has exactly one match. Mutation proposal start/retry events publish sanitized status so interactive users see progress before hidden model correction or broad preview generation completes. Repairable schema-shape failures, missing/ambiguous expected text, and pre-mutation diagnostics are fed back as bounded mutation-proposal correction evidence, controlled by `execution:maxMutationProposalRepairAttempts` and defaulting to three attempts, and publish sanitized retry status before failing closed. Sanitized reasoning is separate, duplicate/out-of-phase calls fail, every mutation path, lifecycle kind, and move endpoint must match its claimed schema-2 file-intent plan steps, and model-owned approval claims are replaced with explicit or policy host review.
- Plan proposals are bounded host-owned schema-2 contracts that declare structured `fileIntents` for modify/create/delete/move/rename path scope and pass cheap repository sanity checks before review or policy auto-approval; unavailable sanity evidence can require manual review but can never feed policy auto-approval. Repairable file-intent path/scope failures trigger bounded plan revision, including unique bare-name normalization and directory scope, so every approved contract retains exact canonical file paths. Hard path/protected-policy failures fail closed. Sanity checks use the active workspace's prohibited paths, treat hashed baseline files as a partial content snapshot, supplement existence with metadata-only confined inspection, reject existing reparse components, and classify lifecycle risk from the structured file-intent kind rather than prose heuristics. Repair turns publish and reuse sanitized repair instructions, and accrue model usage and wall-clock budget. Durable proposals remain pending until approval provenance is recorded and `ApprovalGranted` advances replay state. `/plan-policy` and `planning:approvalPolicy` control only plan approval and remain separate from mutation approval. Mutation proposals are bounded host-owned DTOs tied to one immutable `WorkspaceBaseline`; `TrustedRead` permits private preview/staging, while commit requires `TrustedMutation` plus a matching explicit or host policy approval. `ReviewAll` is default for both plan and mutation policy; plan auto-approval and exact-diff policy auto-approval never suppress durable projection and never weaken confinement, baseline, prohibited/secret-path, Git-metadata, pre-mutation Roslyn, transaction, or validation checks.
- Transactional reads observe immutable baseline content; staged content is private until commit. The workspace coordinator registers typed session/mutation-set review ownership before `MutationSetProposed` becomes observable, so review surfaces resolve the exact staged set without checkpoint polling or timing assumptions. Hash, approved-root, prohibited-path, and reparse-point checks fail closed before writes and again at commit. Structured create/delete/move operations bind exact source identities and absent destinations, include both move endpoints in accepted-plan/worker scope, expose exact lifecycle previews/risk, support explicit encoding/newlines and move-plus-edit, and use compensating application plus protected rollback without a direct filesystem tool. After each reconciled execution apply, the coordinator promotes resulting bytes to the next transactional baseline generation while the original diagnostic baseline remains immutable.
- Advanced semantic inspection reuses the existing per-workspace Roslyn solution. Each query captures one monotonically generation-fenced snapshot, requires `PartialCompilation` or better, reports confidence/completeness/omissions, and fails stale late results. Call/impact traversals are bounded and explainable rather than whole-program proof; C# patterns are inert closed version-1 data; generated queries inspect only already-loaded classified documents and never run generators.
- Validation builds require `TrustedBuild`, invoke `dotnet` directly with no implicit restore, remain confined to the workspace, and publish host-owned structured diagnostics. General build/analyzer/format/package/test tools reuse the tracked process and normalization owners but remain explicitly exploratory; only the governed mutation-validation pipeline can produce authoritative acceptance evidence. Baseline/introduced claims are authoritative only at `FullSemantic`; degraded possibly introduced errors require human confirmation.
- Validation tests are part of the compiled default `validation:stages` alongside semantic, compile, and diagnostics stages; when enabled they require `TrustedBuild`, select affected or directly referencing test projects with host-authored rationale, enumerate and execute without restore/build through the tracked process manager, and publish normalized host-owned test evidence. Semantic validation provides fast source-file diagnostics from the loaded Roslyn workspace over the affected project set, with mutation-touched source documents refreshed from disk and created/deleted source documents structurally added or removed before compilation; semantic baseline capture records pre-mutation affected-project source-file errors so unchanged baseline diagnostics remain non-blocking. Project-level/no-file diagnostics remain build-backed validation evidence, not semantic-only mutation correction evidence. Required semantic-only validation fails closed when its resolver/workspace/Roslyn operation is unavailable, while skipped process validation produces explained completed evidence only when stages are explicitly narrowed. Interrupted post-apply TUI validation retains its exact run guard and remains explicitly resumable through `/validation retry`. Execution checkpoints persist stable identities and artifact references around these boundaries; mutation authorization consumption waits for the review-ready `MutationApprovalPending` checkpoint so an early review event cannot restore a start-request artifact as active execution state; continuation and resume consumption are serialized through one reference-counted per-run gate that is reclaimed after its final holder or waiter, cancelled model turns retain durable host request state, pending effects are reconciled or fail closed rather than blindly replayed, and every resume rehashes the live workspace, trust, and selected solution. A lifecycle commit failure records `RolledBack` only when every endpoint proves `NotStarted`/`Compensated`; cancelled compensated work resumes at fresh mutation authorization, while every reconciliation ending in `Failed` records and signals a terminal outcome—including the current failed operation's authoritative lifecycle reconciliation records—so run waiters cannot hang. Conflicted/indeterminate effects require explicit recovery, and a proven applied mutation replays validation only. Final outcomes mark proposal-correlated plan steps complete only when the complete staged mutation set was authoritatively applied, and retain cumulative diff evidence across corrections.
- Every model-stream consumer enforces a host-owned resource-safety output bound before retaining or publishing text, reasoning, or structured output. The OpenAI-compatible adapter uses bounded linear builders for fragmented tool identifiers, names, and arguments and rejects cumulative streamed output beyond an independent character ceiling; token estimates remain accounting estimates, not exact response limits. Conversational execution applies the configured execution ceiling cumulatively across all model continuation rounds and across text, reasoning, plan output, and tool names/arguments; tool name/argument accounting occurs before argument JSON parsing, a cumulative tool-call count separately bounds structural allocations, and structured plans pass their field/count validator before allocation-free bounded character accounting.

## Verification

- `dotnet build src/Threadsmith.sln` — solution builds cleanly.
- `dotnet test --project tests/Threadsmith.Architecture.Tests/Threadsmith.Architecture.Tests.csproj` — dependency direction tests pass.
- `dotnet test --project tests/Threadsmith.Milestone1.Tests/Threadsmith.Milestone1.Tests.csproj` — command/event fan-out, transition matrix, persistence, sanitization, fake-model, inline-shell parity, exact bulk input, cancellation, and quit-lifecycle contracts pass.
- `dotnet test --project tests/Threadsmith.Milestone2.Tests/Threadsmith.Milestone2.Tests.csproj` — repository trust, discovery, baseline, persistence, projection, and surface contracts pass.
- `dotnet test --project tests/Threadsmith.Milestone3.Tests/Threadsmith.Milestone3.Tests.csproj` — semantic confidence, model providers, tool policy/persistence/UI, and process cancellation contracts pass.
- `dotnet test --project tests/Threadsmith.Milestone4.Tests/Threadsmith.Milestone4.Tests.csproj` — governed context, prompt assets, model resolution, and structured plan review contracts pass.
- `dotnet test --project tests/Threadsmith.Milestone5.Tests/Threadsmith.Milestone5.Tests.csproj` — transactional preview/approval/conflict/rollback/worktree and semantic mutation contracts pass.
- `dotnet test --project tests/Threadsmith.Milestone6.Tests/Threadsmith.Milestone6.Tests.csproj` — affected builds, diagnostics, confidence classification, test discovery/selection/execution, combined gates, cancellation, correction budgets, and validation projections pass.
- `tests\Threadsmith.Milestone11.Tests\bin\Debug\net10.0\Threadsmith.Milestone11.Tests.exe` — approved-plan staging/application, write-ahead exactly-once denial, fail-closed pending-effect resume, and checkpoint migration pass.
- `tests\Threadsmith.Milestone11_1.Tests\bin\Debug\net10.0\Threadsmith.Milestone11_1.Tests.exe` — bounded in-process child scheduling, cancellation/generation fencing, validation/partitioning, authority and budget narrowing, migration 4, conflicts, and real managed worktrees pass.
- `tests\Threadsmith.Milestone12.Tests\bin\Debug\net10.0\Threadsmith.Milestone12.Tests.exe` — metadata-only discovery, maintained packages, trust/enable/revocation, tamper/archive/schema/context/compatibility denial, typed workflow continuation, and migration-5 provenance/pins/checkpoints pass.
- `tests\Threadsmith.Milestone7_4.Tests\bin\Debug\net10.0\Threadsmith.Milestone7_4.Tests.exe` — archive/memory contracts, migration/restoration/retention, compaction/retrieval/invalidation, conversation modes, pressure, security, inspection, and surface parity pass.
- `dotnet test --project tests/Threadsmith.Milestone8.Tests/Threadsmith.Milestone8.Tests.csproj` — migration safety, event/schema-version tolerance (gap #3), content-addressed artifacts, retention, session round-trip, MCP adapter lifecycle/secret-scope/drain-kill (gap #6), diagnostic-bundle canary gate, and redaction audit contracts pass. (Run the built `tests\Threadsmith.Milestone8.Tests\bin\Debug\net10.0\Threadsmith.Milestone8.Tests.exe` directly; the `dotnet test` wrapper reports zero tests for xUnit.v3 MTP projects.)
- `tests\Threadsmith.Milestone9.Tests\bin\Debug\net10.0\Threadsmith.Milestone9.Tests.exe` — real SDK stdio connect/import/invoke/disconnect and hung-process termination plus HTTP/SSE mapping, scoped headers, and network policy pass; live HTTP skips unless its documented environment variables are supplied.
- `tests\Threadsmith.Milestone10.Tests\bin\Debug\net10.0\Threadsmith.Milestone10.Tests.exe` — host-owned OAuth browser/callback UX, SDK option isolation, per-profile access/refresh/expiry cache fields, headless callback input, and fail-closed configuration pass.
- `tests\Threadsmith.Milestone23.Tests\bin\Debug\net10.0\Threadsmith.Milestone23.Tests.exe` — one-authority MCP lifecycle, disconnected/ineligible projection, serialized idempotence, availability/schema identity, explicit untrusted resource/prompt content, identity confirmation, diagnostics, shared headless JSON, and real full-capability stdio coverage pass.

## Child DOX Index

| Child | Scope |
|---|---|
| `Threadsmith.App/AGENTS.md` | Composition-root startup phases, ordering, resource ownership, and shell coordination |
| `Threadsmith.Skills/AGENTS.md` | Metadata-first catalogs, trust/verification, safe schemas/content, declarative workflows, maintained packages, and recovery |
| `Threadsmith.Scripting.Worker/AGENTS.md` | Disposable Roslyn scripting worker, bounded protocol, restrictions, and process isolation |
| `Threadsmith.Models/AGENTS.md` | Host-owned model contracts, configured profiles, selection, and provider-neutral dispatch |
| `Threadsmith.Models.OpenAiCompatible/AGENTS.md` | Compiled OpenAI-compatible configuration, registration, HTTP/SSE adapter, and legacy migration |
| `Threadsmith.Models.OpenAiCodex/AGENTS.md` | Authenticated Codex model discovery, independent OAuth, native Responses/SSE adapter, and metadata cache |
| `Threadsmith.Context/AGENTS.md` | Governed evidence, bounded conversation memory/assembly, prompt assets, and per-request model resolution |
| `Threadsmith.DotNet/AGENTS.md` | MSBuild/Roslyn discovery, confidence lifecycle, pre-mutation analysis, and semantic mutations |
| `Threadsmith.Tools/AGENTS.md` | Tool contracts, policy pipeline, built-ins, secrets, and process lifecycle |
| `Threadsmith.Mcp/AGENTS.md` | SDK-isolated transports, lifecycle authority, bounded capability mapping, OAuth identity, and imported tools |
| `Threadsmith.Tui/AGENTS.md` | Inline conversation lifecycle, native transcript behavior, input, rendering, and review prompts |
| `Threadsmith.Workspaces/AGENTS.md` | Repository lifecycle, immutable baselines, transactional mutation, and Git-worktree isolation |
| `Threadsmith.Validation/AGENTS.md` | Trusted builds, normalized diagnostics, confidence classification, correlation, correction bounds, and gates |
| `Threadsmith.Hooks/AGENTS.md` | Lifecycle hook authority, policy, adapters, bounds, audit, and recursion |

Create additional per-project AGENTS.md files when a project accumulates durable conventions beyond the shared guardrails.
