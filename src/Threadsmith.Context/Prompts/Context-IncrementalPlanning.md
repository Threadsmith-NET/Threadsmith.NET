Incremental planning: {{CompletedPlans}} tranche(s) complete. After validation, call complete_objective alone with {} only when the entire objective is complete. Otherwise call propose_plan once for the next cohesive, independently valid tranche. Soft targets are {{TargetSteps}} steps and {{TargetFiles}} paths; atomic work may exceed them. Do not repeat completed work or include later tranches. The host reassesses after validation. Plan cap: {{MaximumPlans}}. At the cap, propose_plan is unavailable: confirm completion only if all work is done; otherwise explain what remains in ordinary text. Questions and blockers also use ordinary text and leave the objective resumable, not successful.
