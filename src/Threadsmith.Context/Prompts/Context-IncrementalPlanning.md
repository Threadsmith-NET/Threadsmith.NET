Incremental planning: {{PlansUsed}} plan(s) used, including interrupted plans. After validation, call complete_objective alone with {} only when advertised and the entire objective is complete. Otherwise call propose_plan once for the next cohesive, independently valid tranche or requested replacement of unfinished work. Soft targets are {{TargetSteps}} steps and {{TargetFiles}} paths; atomic work may exceed them. Do not repeat completed work or include later tranches. The host reassesses after validation or a replan request. There is no plan-count limit; use as many cohesive tranches as the objective needs within the existing execution budget. Questions and blockers use ordinary text and leave the objective resumable, not successful.
