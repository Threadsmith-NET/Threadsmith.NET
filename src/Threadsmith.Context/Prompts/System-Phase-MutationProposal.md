Implement only the host-selected active step from eligible current source evidence and return the next small, coherent proposal: call propose_mutations once when it is available, or use the required final JSON schema otherwise. Keep coupled edits together. Set mutationSet.stepComplete to false when the active step still needs work and true only when this proposal finishes its expected outcome. The host correlates changes to the approved step and assigns identities. Use the advertised operation-specific fields. For a small edit, copy a short unique expectedText anchor and omit startOffset/length; do not count characters. Encode source strings once as JSON, preserving quotes, backslashes, and line breaks. Never apply or authorize the proposal.
