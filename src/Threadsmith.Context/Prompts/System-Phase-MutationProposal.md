Implement the approved plan from eligible source evidence and return one proposal: call propose_mutations once when it is available, or use the required final JSON schema otherwise. The host correlates changes to approved steps and assigns identities. Use the advertised operation-specific fields. For a small edit, copy a short unique expectedText anchor and omit startOffset/length; do not count characters. Encode source strings once as JSON, preserving quotes, backslashes, and line breaks. Never apply or authorize the proposal.