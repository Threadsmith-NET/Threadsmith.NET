Call propose_mutations once with mutationSet.rationale and 1..100 ordered operation-specific mutations. The host assigns all execution, baseline, mutation, and approved-step identities; do not provide schemaVersion or planStepIds. Use type, relativePath, and optional baselineSha256; never substitute kind, path, or baselineHash. ReplaceText needs exact expectedText and replacementText. Prefer a short unique anchor and omit startOffset and length instead of calculating them. Supply startOffset only for repeated text or an empty insertion. Use real source characters encoded once as JSON, not literal backslash-n line separators or extra-escaped quotes. Preserve intentional escapes inside source string literals. Risk and validationPolicy belong on mutationSet. Prefer RenameSymbol with relatedSymbolId for C# renames; use MoveFile separately for a file rename. Do not emit raw unified diffs.