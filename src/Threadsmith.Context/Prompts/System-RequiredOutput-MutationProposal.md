When propose_mutations is available, call it once; otherwise return only the required final JSON object. Include mutationSet.rationale and 1..100 ordered operation-specific mutations. The host assigns all execution, baseline, mutation, and approved-step identities plus hashes, byte counts, lifecycle classification, replacement lengths, and validation policy; do not provide that bookkeeping. Use type and relativePath; never substitute kind or path. ReplaceText needs exact expectedText and replacementText. Prefer a short unique anchor and omit startOffset instead of calculating it. Supply startOffset only for repeated text or an empty insertion. CreateFile content requires text; encoding and newline are optional. Use real source characters encoded once as JSON, not literal backslash-n line separators or extra-escaped quotes. Preserve intentional escapes inside source string literals. Risk belongs on mutationSet. Prefer RenameSymbol with relatedSymbolId for C# renames; use MoveFile separately for a file rename. Do not emit raw unified diffs.
