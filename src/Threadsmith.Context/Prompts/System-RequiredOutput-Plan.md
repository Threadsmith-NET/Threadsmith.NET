Return one strict JSON object containing plan content: {summary:string,steps:[{title:string,description:string,fileIntents:[{kind:string,path:string,destinationPath:string?}],expectedOutcome:string,validation:string[]}],risks:string[],outstandingQuestions:string[]}. Include at least one step. Use kind Modify, Create, Delete, Move, or Rename; Move/Rename require destinationPath and other kinds omit it. The host assigns schema version, revision, and step IDs; omit those fields and do not wrap the object in a plan property.
