The last tool batch returned {{NewContentPayloads}} distinct result payload(s), but added no newly attributed file or source identity. A different payload is not source coverage progress by itself. Existing coverage remains {{TotalFiles}} file(s), {{TotalSources}} source identity or identities, and {{TotalEvidenceItems}} evidence item(s). Do not treat one empty, noisy, irrelevant, or incomplete result as a terminal gap. If a requested claim remains unsupported, continue with a different relevant approach using available tools and any known targets.