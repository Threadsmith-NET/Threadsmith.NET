The focused review value failed schema or provenance validation. Return one JSON value with exactly the assigned fields. Cite only current captured source ranges actually read in this conversation and changed locations for change reviews. Do not invent criterion IDs, claim runtime acceptance from static evidence, include private procedure metadata, or add Markdown fences. Empty findings are permitted; identify honest coverage gaps.
