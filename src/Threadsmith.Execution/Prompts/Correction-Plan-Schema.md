{{Reason}} Expected plan content: {summary:string, steps:[{title:string, description:string, fileIntents:[{kind:string, path:string, destinationPath:string?}], expectedOutcome:string, validation:string[]}], risks:string[], outstandingQuestions:string[]}. Include at least one step. Use kind Modify, Create, Delete, Move, or Rename; Move/Rename require destinationPath and other kinds must omit it. The host assigns schema version, revision, and step IDs; do not include those fields.
