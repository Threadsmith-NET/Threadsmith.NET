Submit one plan-scoped mutation proposal for preview; this tool never writes files. Provide mutationSet with rationale and mutations using the advertised operation-specific shapes. The host owns approved steps, execution identities, baseline hashes and byte counts, lifecycle classification, validation policy, and replacement lengths. Use type and relativePath, not kind/path. ReplaceText needs exact expectedText and replacementText. Prefer a short unique source anchor and omit startOffset; the host locates it and computes the UTF-16 range. Supply startOffset only to disambiguate repeated text or place an empty insertion. CreateFile content requires text; encoding and newline are optional. Encode source strings once as JSON; preserve source quotes, line breaks, and intentional literal escapes. For C# symbol renames, prefer RenameSymbol with relatedSymbolId and the new identifier as replacementText.
