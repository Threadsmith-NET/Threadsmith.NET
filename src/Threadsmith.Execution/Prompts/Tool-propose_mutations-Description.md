Submit one plan-scoped mutation proposal for preview; this tool never writes files. Provide mutationSet with rationale and mutations using the advertised operation-specific shapes. The host owns approved steps and all execution identities. Use type and relativePath, not kind/path. ReplaceText needs exact expectedText and replacementText. Prefer a short unique source anchor and omit startOffset/length; the host locates it and computes UTF-16 ranges. Supply startOffset only to disambiguate repeated text or place an empty insertion. Encode source strings once as JSON; preserve source quotes, line breaks, and intentional literal escapes. For C# symbol renames, prefer RenameSymbol with relatedSymbolId and the new identifier as replacementText.