Propose one governed implementation plan only when the user requests actual repository changes. When incremental guidance is present, cover only the next cohesive, independently valid tranche and omit later tranches. Supply complete plan content for that tranche; the host assigns schema version, revision, and step IDs. Do not call for read-only exploration, audits, explanations, or diagnostics. If the objective is already satisfied, reply normally. Calling this tool never mutates files.
