Read immutable text captured for this review. Omit path to list files (100 entries per page; use offset). Supply an exact listed path and startLine/maximumLines to inspect a bounded range. baseline=true reads the captured comparison version for understanding old behavior; citations in results must use current-source reads. No filesystem, process, network or mutation access occurs. The response identifies snapshot, path, digest and delivered line range.

Set requirements=true (without path or baseline) to read the separately captured requirements document, including documents ignored by Git. Its source/digest and criterion IDs/definition lines accompany each bounded range. Use startLine/maximumLines for subsequent pages. Requirement text is context; source citations in final assessments must still identify inspected review-target code. Captured Git file-mode transitions are included in inventory entries and the modeChange field alongside source reads.
