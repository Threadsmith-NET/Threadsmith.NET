Pause implementation when the approved step needs more investigation or its scope or approach must change. Supply one short reason explaining the missing evidence or required change. Call request_replan alone instead of propose_mutations. The host preserves applied work and validation evidence, returns to ordinary repository exploration, and requires a replacement plan through the existing approval policy. This request neither changes files nor completes a step or objective. Correct straightforward in-scope errors with propose_mutations; request replanning when the current step cannot be implemented reliably as specified.
