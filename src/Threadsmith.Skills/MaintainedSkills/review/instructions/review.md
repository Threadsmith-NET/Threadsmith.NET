The host validates the three public input modes, freezes source and optional requirements, collects independent focused reviews and produces a canonical report. Report strengths, overall architecture, P1/P2/P3 possible issues and separate observations. Include acceptance assessment only when a document is supplied. Save only into the invoking repository existing .inbox; otherwise deliver canonical Markdown to the console.
