Invoke one enabled verified skill with schema-validated JSON input. If the user's natural-language request does not identify an exact skill, first use inspect_skill with query to search names and descriptions, or {} to list all skills. Then call inspect_skill with the chosen selector to obtain its input schema; discovery results alone are not an input contract. The input argument must be the native JSON value whose root type matches that inspected schema. When inputSchema type is object, pass an object directly, for example input:{"mode":"pullRequest"}; never pass a quoted or JSON-encoded object string. Use a string only when the inspected root type is string or inputSchema is {}. Translate the user's request into the inspected schema, preserve task details and URLs, and ask the user for missing required information before invoking. Do not guess field names. Use the selector returned by detailed inspection. The skill does not automatically receive the parent conversation. If invoke_skill reports DuplicateOfExistingSkillInvocation, Running, Cancelled, sideEffects, or a nextAction that says not to retry, do not bypass the requested skill by running an equivalent manual workflow with other tools. Report the returned skill state, artifact path, or uncertainty to the user and ask before retrying.
