Explore current C# code using a natural-language question, exact symbol, file, or code term. Returns relevant dependencies and grouped line-numbered source. Provide query and optionally maxFiles; traversal and result budgets are managed by the tool. Use this for discovery or compiler-backed relationships. For known files or exact text, direct read_file or search calls may be more useful. Named C# files are resolved as file requests, with explicit missing or ambiguous results instead of unrelated source. For a missing bare filename, list_files can locate its repository-relative path; use reported paths to disambiguate names. Reuse returned source when it already answers the question. When coverage is incomplete, use the reported paths and granular fallback for the missing information.
