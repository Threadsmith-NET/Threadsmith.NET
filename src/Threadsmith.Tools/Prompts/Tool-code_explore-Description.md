Explore unfamiliar or cross-cutting C# behavior using a focused natural-language question or a small set of symbol or file anchors. Returns bounded current line-numbered source grouped by file, with relevant multi-hop flow, dependencies, impact, coverage, and omissions. Use when the relevant code is unknown or the question spans relationships that one targeted semantic operation cannot establish. For a known declaration or direct relationship, use find_symbol and the matching relationship tool; for a known repository-relative file's contents, use read_file; for exact text, use search. Treat returned source ranges as already read and do not reopen or re-search them. Keep query no broader than the user's request; maxFiles is optional, and traversal and result budgets are managed by the tool. Named C# files produce explicit missing or ambiguous results. Use list_files only to resolve a missing bare filename. Follow a reported granular continuation or fallback only when the missing evidence is needed to answer an outstanding question; incomplete coverage alone does not require another call. With no evidence, narrow the query or make one targeted lookup, not a broad inventory.
