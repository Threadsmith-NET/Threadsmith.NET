Compiler-aware lookup for a known or likely C# declaration. Use instead of text search for symbol discovery; its stable symbolId feeds find_references, find_implementations, call_hierarchy, and symbol_impact. Use read_file for a known file's contents. Use code_explore only when relevant symbols are unknown or a broader structural survey is required.
