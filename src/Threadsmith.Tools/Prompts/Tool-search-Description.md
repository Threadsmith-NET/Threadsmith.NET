Search file contents for concise exact literals, keys, routes, log messages, comments, docs, locals, or parameters. query is limited to {{MaximumQueryCharacters}} characters; do not paste source blocks, tool output, or long regexes. maximumMatches is a clamped hint; 0 uses the default. Scope with repository-relative path and glob; omitted, null, blank, or "." path means repository root. Use directly for text in a known file. For C# declarations or compiler-backed references, implementations, calls, impact, or syntax shapes, use the matching narrow semantic tool; text does not prove identity, dispatch, or impact. Use code_explore only for broader unknown discovery. Batch independent searches.
