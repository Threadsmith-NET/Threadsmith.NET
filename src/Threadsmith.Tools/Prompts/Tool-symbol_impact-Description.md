Compiler-aware combined impact of a known C# symbol across references, callers, implementations, projects, tests, and classified source. Pass {symbolId} from find_symbol. Use a narrower relationship tool when only references, implementations, or calls are requested. Use this instead of text search for combined impact; fall back only on failure or incomplete evidence.
